numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.5
rank_bm25>=0.2.2
faiss-cpu>=1.7
# Optional — set RAG_TRANSFORMER=1 to use a real encoder instead of TF-IDF->SVD.
# Listed separately so CI stays light; install manually when desired:
#   pip install sentence-transformers>=2.2
